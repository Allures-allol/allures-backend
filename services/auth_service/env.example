# Один URL для всех сервисов (Supabase)
MAINDB_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require

DB_ECHO=false

JWT_SECRET=changeme
JWT_ALG=HS256
ACCESS_TTL_MIN=15
JWT_ISS=allures-auth
JWT_AUD=allures-api
