# --- DB ---
MAINDB_URL=postgresql+psycopg2://USER:PASSWORD@HOST:5432/DBNAME?sslmode=require
DB_ECHO=false

# --- AUTH ---
JWT_SECRET=super-secret-change-me
JWT_ALG=HS256
ACCESS_TTL_MIN=15
JWT_ISS=allures-auth
JWT_AUD=allures-api

# --- ROOT PATHS (прод) ---
# auth выставлен в коде админом, а для единообразия можно и так:
# ROOT_PATH для каждого сервиса задаётся в его коде или переменной окружения.
# Можно через env:
AUTH_ROOT_PATH=/auth
PRODUCT_ROOT_PATH=/product
REVIEW_ROOT_PATH=/reviews

# --- SERVICE URLS ДЛЯ ВЗАИМОДЕЙСТВИЯ ---
# review_client теперь ожидает URL УЖЕ С ПРЕФИКСОМ /reviews
REVIEW_SERVICE_URL=https://api.alluresallol.com/reviews

# Если где-то планируешь REST-коллы в обратную сторону:
PRODUCT_SERVICE_URL=https://api.alluresallol.com/product
AUTH_SERVICE_URL=https://api.alluresallol.com/auth
